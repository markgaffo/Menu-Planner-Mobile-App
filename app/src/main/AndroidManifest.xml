<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.caproject">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_newlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_newlogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CAProject">
        <activity android:name=".ChinaAddActivity"
            android:parentActivityName=".ChinaRest"/>
        <activity android:name=".ChinaRest"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".LoginActivity" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.CAProject.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".restaurantActivity1"
            android:label="@string/title_activity_restaurant1"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Activity2"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>